---
// src/components/WrappedProjectsContainer.astro
interface Props {
  projects: Array<{ 
    id: number; 
    title: string; 
    description: string; 
    url: string;
  }>;
}

const { projects } = Astro.props;

// Definir diferentes iconos minimalistas para cada proyecto con colores del gradiente macOS
const projectIcons = [
  // Icono 1: Código (My First Web) - Rosa coral
  '<svg class="w-12 h-12 gradient-icon-1" fill="none" stroke="#ff9a9e" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path></svg>',
  // Icono 2: Tienda (Sell In A Flash) - Melocotón
  '<svg class="w-12 h-12 gradient-icon-2" fill="none" stroke="#fad0c4" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path></svg>',
  // Icono 3: Gestión (Maneja Tu Proyecto) - Azul lavanda
  '<svg class="w-12 h-12 gradient-icon-3" fill="none" stroke="#a1c4fd" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01"></path></svg>',
  // Icono 4: Memoria/Foto (Harvard Memories) - Azul cielo
  '<svg class="w-12 h-12 gradient-icon-4" fill="none" stroke="#c2e9fb" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>',
  // Icono 5: Dev.to (DevTo Fun) - Rosa coral intenso
  '<svg class="w-12 h-12 gradient-icon-1" fill="none" stroke="#ff9a9e" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"></path></svg>',
  // Icono 6: Aviación (Airman Project) - Melocotón
  '<svg class="w-12 h-12 gradient-icon-2" fill="none" stroke="#fad0c4" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path></svg>',
  // Icono 7: Gráfico/Datos (Vet Análisis Datos) - Azul lavanda
  '<svg class="w-12 h-12 gradient-icon-3" fill="none" stroke="#a1c4fd" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path></svg>',
  // Icono 8: Pensamiento/Idea (CS50Thoughts) - Azul cielo
  '<svg class="w-12 h-12 gradient-icon-4" fill="none" stroke="#c2e9fb" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path></svg>'
];

// Clases de texto con gradientes para títulos
const gradientTitleClasses = [
  'gradient-text-title-1', // Rosa coral
  'gradient-text-title-2', // Melocotón  
  'gradient-text-title-3', // Azul lavanda
  'gradient-text-title-4', // Azul cielo
  'gradient-text-title-1', // Repetir patrón
  'gradient-text-title-2',
  'gradient-text-title-3', 
  'gradient-text-title-4'
];

// Clases de texto con gradientes para descripciones
const gradientDescClasses = [
  'gradient-text-desc-1', // Rosa coral suave
  'gradient-text-desc-2', // Melocotón suave
  'gradient-text-desc-3', // Azul lavanda suave
  'gradient-text-desc-4', // Azul cielo suave
  'gradient-text-desc-1', // Repetir patrón
  'gradient-text-desc-2',
  'gradient-text-desc-3',
  'gradient-text-desc-4'
];

// Tamaños tipo "bento" deterministas (evita parpadeo al re-render)
function bentoSize(index: number) {
  const sizes = [
    'w-[18rem] h-[18rem]',
    'w-[22rem] h-[18rem]',
    'w-[18rem] h-[24rem]',
    'w-[26rem] h-[20rem]',
    'w-[20rem] h-[20rem]',
    'w-[24rem] h-[26rem]',
    'w-[18rem] h-[22rem]',
    'w-[22rem] h-[24rem]'
  ];
  return sizes[index % sizes.length];
}
---

<!-- Carrusel horizontal infinito -->
<div class="projects-infinite-wrapper relative h-full w-full overflow-hidden">
  <div id="wrapped-projects-container" class="infinite-container scrollbar-hide" style="scrollbar-width:none; -ms-overflow-style:none;">
    <div class="projects-track">
      {(() => {
        // Precomputar tamaños deterministas para consistencia entre duplicados
        const sizes = projects.map((_, i) => bentoSize(i));
        // Duplicar los proyectos para crear el efecto infinito
        const duplicatedProjects = [...projects, ...projects];
        
        return duplicatedProjects.map((project, index) => {
          const originalIndex = index % projects.length;
          return (
            <a 
              href={project.url}
              target="_blank"
              rel="noopener noreferrer"
              class={`project-item preload glass-card group ${sizes[originalIndex]} rounded-3xl shadow-lg transition-all duration-300 overflow-hidden transform hover:-translate-y-1 hover:scale-105`}
              data-index={originalIndex}
              data-duplicate={index >= projects.length ? 'true' : 'false'}
            >
              <!-- Window Controls (macOS style) -->
              <div class="window-controls absolute top-3 left-3 flex space-x-2 z-10 transition-all duration-300">
                <div class="window-control-btn window-control-close"></div>
                <div class="window-control-btn window-control-minimize"></div>
                <div class="window-control-btn window-control-maximize"></div>
              </div>
              
              <div class="project-item-inner flex flex-col items-center justify-center p-6 h-full">
                <div class="project-icon w-16 h-16 rounded-2xl bg-gradient-to-br from-blue-50/50 to-purple-50/50 flex items-center justify-center border-white/20 mb-4">
                  <div set:html={projectIcons[originalIndex % projectIcons.length]} />
                </div>
                <h3 class={`project-title text-lg font-semibold mb-2 text-center leading-tight ${gradientTitleClasses[originalIndex % gradientTitleClasses.length]}`}>{project.title}</h3>
                <p class={`project-desc text-center text-sm line-clamp-3 leading-relaxed ${gradientDescClasses[originalIndex % gradientDescClasses.length]}`}>{project.description}</p>
              </div>
            </a>
          );
        });
      })()}
    </div>
  </div>
  
  <!-- Indicadores de scroll (opcional) -->
  <div class="scroll-indicators absolute bottom-4 left-1/2 transform -translate-x-1/2 flex space-x-2 z-10">
    <div class="scroll-indicator w-2 h-2 bg-white/30 rounded-full"></div>
    <div class="scroll-indicator w-2 h-2 bg-white/30 rounded-full"></div>
    <div class="scroll-indicator w-2 h-2 bg-white/50 rounded-full"></div>
  </div>
</div>

<!-- Botón visual macOS (solo UI) en la esquina inferior derecha -->
<div id="macos-restore-button" class="macos-restore-button" role="button" tabindex="0" aria-label="Restaurar ventanas cerradas" hidden>
  <div class="macos-restore-button-inner">
    <span class="macos-restore-label">Restore</span>
  </div>
</div>

<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const container = document.getElementById('wrapped-projects-container');
    const track = container?.querySelector('.projects-track');
    const actionButton = document.getElementById('macos-restore-button');
    if (!container || !track) return;

    // Array que rastrea los elementos que han recibido la clase 'closed-state'
    const closedProjects = [];

    function updateRestoreButton() {
      if (!actionButton) return;
      if (closedProjects.length > 0) {
        actionButton.removeAttribute('hidden');
        actionButton.classList.add('show');
        actionButton.setAttribute('aria-label', `Restaurar ${closedProjects.length} ventanas`);
        actionButton.style.pointerEvents = 'auto';
      } else {
        actionButton.classList.remove('show');
        actionButton.setAttribute('aria-label', 'Restaurar ventanas cerradas');
        actionButton.style.pointerEvents = 'none';
        actionButton.setAttribute('hidden', '');
      }
    }

    function addClosedProject(projectItem) {
      if (!projectItem) return;
      if (!closedProjects.includes(projectItem)) {
        closedProjects.push(projectItem);
      }
      // Log current closed projects for debugging (count + data-indexes)
      try {
        const indexes = closedProjects.map(p => p.getAttribute && p.getAttribute('data-index'));
        console.log('closedProjects ADDED -> count:', closedProjects.length, 'indexes:', indexes);
      } catch (err) {
        console.log('closedProjects ADDED -> count:', closedProjects.length);
      }
      updateRestoreButton();
    }

    function removeClosedProject(projectItem) {
      const idx = closedProjects.indexOf(projectItem);
      if (idx !== -1) closedProjects.splice(idx, 1);
      // Log current closed projects for debugging (count + data-indexes)
      try {
        const indexes = closedProjects.map(p => p.getAttribute && p.getAttribute('data-index'));
        console.log('closedProjects REMOVED -> count:', closedProjects.length, 'indexes:', indexes);
      } catch (err) {
        console.log('closedProjects REMOVED -> count:', closedProjects.length);
      }
      updateRestoreButton();
    }

    let isPaused = false;
    let animationId = null;
    let isDragging = false;
    let startX = 0;
    let scrollLeft = 0;

    // Scroll automático simple y directo
    function autoScroll() {
      if (!isPaused && !isDragging) {
        container.scrollLeft += 1;

        // Reset cuando llegue al final (simple wrap)
        if (container.scrollLeft >= container.scrollWidth / 2) {
          container.scrollLeft = 0;
        }
      }
      animationId = requestAnimationFrame(autoScroll);
    }

    // Iniciar inmediatamente después de que el DOM esté listo
    setTimeout(() => {
      console.log('Iniciando scroll automático...');
      autoScroll();
    }, 100);

    // Animación de entrada para las project cards: las tarjetas se renderizan
    // inicialmente con la clase `preload` (render server-side) para que el
    // primer paint las muestre fuera de su posición final. Aquí añadimos
    // `.loaded` en un stagger usando rAF para animarlas suavemente hacia su
    // posición original sin parpadeos.
    function animateCardsIn() {
      const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      const cards = Array.from(track.querySelectorAll('.project-item'));

      if (prefersReduced) {
        // Remove preload so they render in place immediately
        cards.forEach(c => c.classList.remove('preload'));
        cards.forEach(c => c.classList.add('loaded'));
        return;
      }

      // Ensure `.preload` is present before scheduling `.loaded`.
      // Then use requestAnimationFrame and a small stagger to add `.loaded`.
      requestAnimationFrame(() => {
        let i = 0;
        const step = () => {
          if (i >= cards.length) {
            // Clean up `preload` after all cards have started animating
            setTimeout(() => cards.forEach(c => c.classList.remove('preload')), 600);
            return;
          }
          cards[i].classList.add('loaded');
          i += 1;
          // small timeout between items to create a pleasing cascade
          setTimeout(() => requestAnimationFrame(step), 40);
        };
        step();
      });
    }

    // Run entrance animation shortly after mount so layout has stabilized
    setTimeout(animateCardsIn, 80);

    // Pausar solo con hover
    container.addEventListener('mouseenter', () => {
      isPaused = true;
      console.log('Pausado');
    });

    container.addEventListener('mouseleave', () => {
      isPaused = false;
      console.log('Reanudado');
    });

    // Wheel scroll
    container.addEventListener('wheel', (e) => {
      e.preventDefault();
      container.scrollLeft += e.deltaY * 2;
    }, { passive: false });

    // Drag functionality
    function startDragging(e) {
      isDragging = true;
      container.style.cursor = 'grabbing';
      startX = (e.pageX || e.touches[0].pageX) - container.offsetLeft;
      scrollLeft = container.scrollLeft;
    }

    function stopDragging() {
      isDragging = false;
      container.style.cursor = 'grab';
    }

    function drag(e) {
      if (!isDragging) return;
      e.preventDefault();
      const x = (e.pageX || e.touches[0].pageX) - container.offsetLeft;
      const walk = (x - startX) * 2;
      container.scrollLeft = scrollLeft - walk;
    }

    // Event listeners
    container.addEventListener('mousedown', startDragging);
    container.addEventListener('touchstart', startDragging, { passive: true });
    document.addEventListener('mouseup', stopDragging);
    document.addEventListener('touchend', stopDragging);
    document.addEventListener('mousemove', drag);
    document.addEventListener('touchmove', drag, { passive: true });

    // Window Controls functionality
    function handleWindowControl(e, action, projectItem) {
      e.preventDefault();
      e.stopPropagation();

      // Remover clases de animación y estado previas
      projectItem.classList.remove('closing', 'minimizing', 'maximizing', 'restoring');
      projectItem.classList.remove('closed-state', 'minimized-state', 'maximized-state');

      switch(action) {
        case 'close':
          projectItem.classList.add('closing');
          setTimeout(() => {
            projectItem.classList.remove('closing');
            projectItem.classList.add('closed-state');
            // Añadir a la lista de cerrados cuando la clase ya está aplicada
            addClosedProject(projectItem);
          }, 600);
          break;

        case 'minimize':
          projectItem.classList.add('minimizing');
          setTimeout(() => {
            projectItem.classList.remove('minimizing');
            projectItem.classList.add('minimized-state');
          }, 600);
          break;

        case 'maximize':
          projectItem.classList.add('maximizing');
          setTimeout(() => {
            projectItem.classList.remove('maximizing');
            projectItem.classList.add('maximized-state');
          }, 400);
          break;
      }
    }

    // Función para restaurar tarjeta desde el borde - USA TRANSICIÓN CSS
    function restoreCard(projectItem) {
      // Remover estados y permitir que la transición CSS haga el trabajo
      projectItem.classList.remove('closed-state', 'minimized-state', 'maximized-state');
      // Limpiar estilos inline aplicados por JavaScript
      projectItem.style.position = '';
      projectItem.style.top = '';
      projectItem.style.left = '';
      projectItem.style.transform = '';
      projectItem.style.zIndex = '';

      // Quitar de la lista de cerrados si estaba ahí
      removeClosedProject(projectItem);
      // La transición CSS automáticamente animará de vuelta al estado original
    }

    // Agregar event listeners a todos los window controls
    document.querySelectorAll('.window-control-btn').forEach(btn => {
      btn.addEventListener('click', (e) => {
        const projectItem = btn.closest('.project-item');

        if (btn.classList.contains('window-control-close')) {
          handleWindowControl(e, 'close', projectItem);
        } else if (btn.classList.contains('window-control-minimize')) {
          handleWindowControl(e, 'minimize', projectItem);
        } else if (btn.classList.contains('window-control-maximize')) {
          handleWindowControl(e, 'maximize', projectItem);
        }
      });
    });

    // Event listener para restaurar tarjetas clickeando en ellas cuando están en los bordes
    document.addEventListener('click', (e) => {
      const projectItem = e.target.closest('.project-item');
      if (projectItem && (
        projectItem.classList.contains('closed-state') ||
        projectItem.classList.contains('minimized-state') ||
        projectItem.classList.contains('maximized-state')
      )) {
        e.preventDefault();
        e.stopPropagation();
        restoreCard(projectItem);
      }
    });

    // Mostrar un feedback simple al pulsar el botón de restaurar (no restaura todo por ahora)
    if (actionButton) {
      actionButton.addEventListener('click', async (ev) => {
        ev.preventDefault();
        if (closedProjects.length === 0) return;

        // Disable button while restoring
        actionButton.style.pointerEvents = 'none';
        actionButton.classList.remove('show');

        // Small pop animation
        actionButton.animate([
          { transform: 'translateY(0)' },
          { transform: 'translateY(-8px)' },
          { transform: 'translateY(0)' }
        ], { duration: 260 });

        console.log('Restore ALL clicked, restoring', closedProjects.length, 'items');

        // Work on a shallow copy because restoreCard will modify the array
        const toRestore = closedProjects.slice();

        for (let i = 0; i < toRestore.length; i++) {
          const item = toRestore[i];
          try {
            // Add a tiny stagger for visibility
            restoreCard(item);
            console.log('Restored item index:', item.getAttribute && item.getAttribute('data-index'));
            // Wait a bit so user sees the individual restores (skip if prefers-reduced-motion)
            if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
              await new Promise(r => setTimeout(r, 120));
            }
          } catch (err) {
            console.error('Error restoring item', err);
          }
        }

        // Ensure array cleared and UI updated
        closedProjects.length = 0;
        updateRestoreButton();
        console.log('All restored, closedProjects count now:', closedProjects.length);

        // Re-enable pointer events (updateRestoreButton may hide the button)
        actionButton.style.pointerEvents = '';
      });
    }

    // Cleanup
    window.addEventListener('beforeunload', () => {
      if (animationId) {
        cancelAnimationFrame(animationId);
      }
    });
  });
</script>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Pixelify+Sans:wght@400;500;600;700&display=swap");

  /* Layout infinito horizontal */
  .projects-infinite-wrapper { 
    height: 100vh; 
    position: relative;
    overflow: hidden;
    font-family: "Pixelify Sans", cursive;
  }
  
  .infinite-container { 
    width: 100%; 
    height: 100%; 
    position: relative; 
    cursor: grab; 
    overflow-x: auto;
    overflow-y: hidden;
    scroll-behavior: smooth;
    scrollbar-width: none;
    -ms-overflow-style: none;
    user-select: none; /* Evita selección de texto durante el drag */
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
  }
  
  .infinite-container::-webkit-scrollbar {
    display: none;
  }
  
  .infinite-container:active { 
    cursor: grabbing; 
  }
  
  .projects-track {
    display: flex;
    flex-direction: row; /* Horizontal layout */
    align-items: flex-start;
    gap: 1.5rem;
    height: 100%;
    width: max-content; /* Permite que el track se extienda horizontalmente */
    padding: 2rem;
    /* Crear columnas de diferentes alturas para efecto masonry */
  }
  
  .project-item { 
    flex: 0 0 auto;
    display: flex;
    align-items: stretch;
    transition: all 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94); /* Transición suave para todos los cambios */
    user-select: none; /* Evita selección de texto */
    -webkit-user-select: none; /* Safari */
    -moz-user-select: none; /* Firefox */
    -ms-user-select: none; /* IE/Edge */
  }

  /* Entrada desde arriba: estado inicial y estado cargado */
  .project-item.preload {
    transform: translateY(-32px) scale(0.995);
    opacity: 0;
    will-change: transform, opacity;
  }

  .project-item.loaded {
    transform: translateY(0) scale(1);
    opacity: 1;
    transition: transform 520ms cubic-bezier(0.22, 1, 0.36, 1), opacity 520ms ease;
  }
  
  .project-item-inner { 
    height: 100%; 
    width: 100%; 
    user-select: none; /* Evita selección de texto en el contenido */
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
  }

  /* Asegurar que títulos y descripciones usen Pixelify Sans */
  .project-title,
  .project-desc {
    user-select: none;
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    font-family: "Pixelify Sans", cursive;
  }

  /* Crear efecto masonry con diferentes alturas */
  .project-item:nth-child(5n+1) { align-self: flex-start; }
  .project-item:nth-child(5n+2) { align-self: center; }
  .project-item:nth-child(5n+3) { align-self: flex-end; }
  .project-item:nth-child(5n+4) { align-self: flex-start; margin-top: 2rem; }
  .project-item:nth-child(5n+5) { align-self: center; margin-top: 1rem; }

  /* Ajustes responsivos: reducir tamaños en pantallas pequeñas */
  @media (max-width: 1024px) {
    .project-item[class*='w-[26rem]'] { width: 20rem !important; }
    .project-item[class*='w-[24rem]'] { width: 18rem !important; }
    .project-item[class*='w-[22rem]'] { width: 16rem !important; }
  }
  
  @media (max-width: 768px) {
    .project-item { 
      width: 14rem !important; 
      height: 16rem !important; 
    }
    .projects-track {
      padding: 1rem;
      gap: 1rem;
    }
  }

  /* Estilo de tarjeta glass mejorado - Mayor contraste y legibilidad */
  .glass-card {
    background: rgba(255, 255, 255, 0.35); /* Más opaco para mejor contraste */
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    border: 1px solid rgba(255, 255, 255, 0.4); /* Borde más visible */
    font-family: "Pixelify Sans", cursive;
    box-shadow: 
      0 12px 40px rgba(0, 0, 0, 0.15), /* Sombras más pronunciadas */
      0 6px 20px rgba(0, 0, 0, 0.08),
      inset 0 1px 0 rgba(255, 255, 255, 0.5), /* Highlight más fuerte */
      inset 0 -1px 0 rgba(0, 0, 0, 0.1); /* Borde inferior sutil */
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .glass-card:hover {
    background: rgba(255, 255, 255, 0.45); /* Aún más opaco en hover */
    transform: translateY(-8px) scale(1.02);
    box-shadow: 
      0 20px 60px rgba(0, 0, 0, 0.2), /* Sombras más dramáticas */
      0 10px 30px rgba(0, 0, 0, 0.12),
      inset 0 1px 0 rgba(255, 255, 255, 0.6),
      inset 0 -1px 0 rgba(0, 0, 0, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.6); /* Borde más brillante en hover */
  }
  
  /* Efecto hover para el icono */
  .project-icon {
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  
  .glass-card:hover .project-icon {
    transform: scale(1.15) rotate(8deg);
    background: linear-gradient(135deg, rgba(239, 246, 255, 0.8), rgba(245, 243, 255, 0.8));
    border-color: rgb(255, 255, 255);
  }
  
  /* Efecto hover para el título */
  .project-title {
    transition: all 0.3s ease;
  }
  
  .glass-card:hover .project-title {
    transform: translateY(-2px);
    color: #4f46e5;
    text-shadow: 0 2px 8px rgba(79, 70, 229, 0.3);
  }
  
  /* Limitar el número de líneas en la descripción */
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  /* Estilos para macOS-inspired design */
  .glass-card {
    border-radius: 20px;
    position: relative;
    overflow: hidden;
  }
  
  /* Efecto de borde sutil superior */
  .glass-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, 
      transparent, 
      rgba(255,255,255,0.8), 
      transparent
    );
    pointer-events: none;
  }
  
  /* Indicadores de scroll */
  .scroll-indicators {
    opacity: 0.6;
    transition: opacity 0.3s ease;
  }
  
  .projects-infinite-wrapper:hover .scroll-indicators {
    opacity: 1;
  }
  
  .scroll-indicator {
    transition: all 0.3s ease;
  }
  
  /* Animación sutil de entrada para las tarjetas - DESHABILITADA */
  .project-item {
    /* animation: slideInFromRight 0.6s ease-out; */
    /* animation-fill-mode: both; */
  }
  
  /* .project-item:nth-child(1) { animation-delay: 0.1s; }
  .project-item:nth-child(2) { animation-delay: 0.2s; }
  .project-item:nth-child(3) { animation-delay: 0.3s; }
  .project-item:nth-child(4) { animation-delay: 0.4s; }
  .project-item:nth-child(5) { animation-delay: 0.5s; } */
  
  @keyframes slideInFromRight {
    from {
      opacity: 0;
      transform: translateX(50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
  
  /* Mejoras para accesibilidad */
  @media (prefers-reduced-motion: reduce) {
    .project-item,
    .glass-card,
    .project-icon,
    .project-title {
      animation: none !important;
      transition: none !important;
    }
  }
  
  /* Estados de focus para accesibilidad */
  .glass-card:focus-visible {
    outline: 2px solid #4f46e5;
    outline-offset: 4px;
  }

  /* Prevenir selección de texto durante el drag en toda la aplicación */
  .infinite-container.dragging,
  .infinite-container.dragging * {
    user-select: none !important;
    -webkit-user-select: none !important;
    -moz-user-select: none !important;
    -ms-user-select: none !important;
  }

  /* Prevenir selección de texto en todos los elementos de proyecto */
  .project-item *,
  .glass-card *,
  .project-icon *,
  .project-title,
  .project-desc {
    user-select: none !important;
    -webkit-user-select: none !important;
    -moz-user-select: none !important;
    -ms-user-select: none !important;
  }

  /* Estilos simplificados para el borde del contenedor de iconos */
  .glass-card:hover .project-icon {
    border-color: rgba(255, 255, 255, 0.6) !important;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.2), rgba(255, 255, 255, 0.1));
    transform: scale(1.15) rotate(8deg);
    box-shadow: 0 4px 16px rgba(255, 255, 255, 0.3);
  }
  
  /* Efectos hover para iconos SVG - cambiar stroke */
  .glass-card:hover svg.gradient-icon-1 {
    stroke: #ff6b6b !important;
    filter: drop-shadow(0 4px 8px rgba(255, 154, 158, 0.4));
  }
  
  .glass-card:hover svg.gradient-icon-2 {
    stroke: #ffc048 !important;
    filter: drop-shadow(0 4px 8px rgba(250, 208, 196, 0.4));
  }
  
  .glass-card:hover svg.gradient-icon-3 {
    stroke: #667eea !important;
    filter: drop-shadow(0 4px 8px rgba(161, 196, 253, 0.4));
  }
  
  .glass-card:hover svg.gradient-icon-4 {
    stroke: #764ba2 !important;
    filter: drop-shadow(0 4px 8px rgba(194, 233, 251, 0.4));
  }

  /* Estilos de gradientes macOS para títulos - Legibilidad mejorada */
  .gradient-text-title-1 {
    background: linear-gradient(45deg, #ff9a9e, #ff6b6b);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    font-family: "Pixelify Sans", cursive;
    font-weight: 800; /* Más bold para mejor contraste */
    font-size: 1.25rem; /* Más grande para mejor legibilidad */
    text-shadow: 
      0 2px 4px rgba(255, 154, 158, 0.5),
      0 4px 8px rgba(255, 107, 107, 0.3),
      0 1px 2px rgba(0, 0, 0, 0.1); /* Sombra negra sutil para contraste */
    letter-spacing: 0.03em;
    transition: all 0.3s ease;
  }
  
  .gradient-text-title-2 {
    background: linear-gradient(45deg, #fad0c4, #ffc048);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    font-family: "Pixelify Sans", cursive;
    font-weight: 800;
    font-size: 1.25rem;
    text-shadow: 
      0 2px 4px rgba(250, 208, 196, 0.5),
      0 4px 8px rgba(255, 192, 72, 0.3),
      0 1px 2px rgba(0, 0, 0, 0.1);
    letter-spacing: 0.03em;
    transition: all 0.3s ease;
  }
  
  .gradient-text-title-3 {
    background: linear-gradient(45deg, #a1c4fd, #667eea);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    font-family: "Pixelify Sans", cursive;
    font-weight: 800;
    font-size: 1.25rem;
    text-shadow: 
      0 2px 4px rgba(161, 196, 253, 0.5),
      0 4px 8px rgba(102, 126, 234, 0.3),
      0 1px 2px rgba(0, 0, 0, 0.1);
    letter-spacing: 0.03em;
    transition: all 0.3s ease;
  }
  
  .gradient-text-title-4 {
    background: linear-gradient(45deg, #c2e9fb, #764ba2);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    font-family: "Pixelify Sans", cursive;
    font-weight: 800;
    font-size: 1.25rem;
    text-shadow: 
      0 2px 4px rgba(194, 233, 251, 0.5),
      0 4px 8px rgba(118, 75, 162, 0.3),
      0 1px 2px rgba(0, 0, 0, 0.1);
    letter-spacing: 0.03em;
    transition: all 0.3s ease;
  }

  /* Estilos de gradientes macOS para descripciones - Legibilidad mejorada */
  .gradient-text-desc-1 {
    background: linear-gradient(45deg, #ff9a9e, #ff7a85);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    font-family: "Pixelify Sans", cursive;
    font-weight: 600; /* Más peso para mejor visibilidad */
    font-size: 0.95rem; /* Ligeramente más grande */
    line-height: 1.6; /* Mejor interlineado */
    letter-spacing: 0.025em; /* Espaciado mejorado */
    text-shadow: 
      0 2px 4px rgba(255, 154, 158, 0.4),
      0 1px 2px rgba(0, 0, 0, 0.08); /* Sombra negra sutil */
    transition: all 0.3s ease;
  }
  
  .gradient-text-desc-2 {
    background: linear-gradient(45deg, #fad0c4, #f2b366);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    font-family: "Pixelify Sans", cursive;
    font-weight: 600;
    font-size: 0.95rem;
    line-height: 1.6;
    letter-spacing: 0.025em;
    text-shadow: 
      0 2px 4px rgba(250, 208, 196, 0.4),
      0 1px 2px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
  }
  
  .gradient-text-desc-3 {
    background: linear-gradient(45deg, #a1c4fd, #7b9cf0);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    font-family: "Pixelify Sans", cursive;
    font-weight: 600;
    font-size: 0.95rem;
    line-height: 1.6;
    letter-spacing: 0.025em;
    text-shadow: 
      0 2px 4px rgba(161, 196, 253, 0.4),
      0 1px 2px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
  }
  
  .gradient-text-desc-4 {
    background: linear-gradient(45deg, #c2e9fb, #9ec8e8);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    font-family: "Pixelify Sans", cursive;
    font-weight: 600;
    font-size: 0.95rem;
    line-height: 1.6;
    letter-spacing: 0.025em;
    text-shadow: 
      0 2px 4px rgba(194, 233, 251, 0.4),
      0 1px 2px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
  }

  /* Efectos hover para textos - Más oscuro para mejor legibilidad */
  .glass-card:hover .gradient-text-title-1,
  .glass-card:hover .gradient-text-title-2,
  .glass-card:hover .gradient-text-title-3,
  .glass-card:hover .gradient-text-title-4 {
    transform: translateY(-2px);
    scale: 1.05;
  }
  
  .glass-card:hover .gradient-text-desc-1,
  .glass-card:hover .gradient-text-desc-2,
  .glass-card:hover .gradient-text-desc-3,
  .glass-card:hover .gradient-text-desc-4 {
    scale: 1.05;
  }

  /* macOS Window Controls */
  .window-controls {
    pointer-events: auto; /* Permitir interacciones */
  }
  
  .window-control-btn {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    transition: all 0.2s ease;
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.3), 0 1px 2px rgba(0, 0, 0, 0.3);
    position: relative;
    border: 0.5px solid rgba(0, 0, 0, 0.1);
    cursor: pointer; /* Hacer clickeable */
  }
  
  /* Colores característicos de macOS */
  .window-control-close {
    background: #ff5f57;
    background: linear-gradient(135deg, #ff6058 0%, #ff4d4d 100%);
  }
  
  .window-control-minimize {
    background: #ffbd2e;
    background: linear-gradient(135deg, #ffca2c 0%, #ffb000 100%);
  }
  
  .window-control-maximize {
    background: #28ca42;
    background: linear-gradient(135deg, #32d74b 0%, #28a745 100%);
  }
  
  .window-control-btn:hover {
    transform: scale(1.1);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.4), 0 2px 4px rgba(0, 0, 0, 0.4);
  }
  
  /* Iconos dentro de los botones (aparecen solo en hover) */
  .window-control-close:hover::after {
    content: '✕';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: #8B1538;
    font-size: 8px;
    font-weight: bold;
  }
  
  .window-control-minimize:hover::after {
    content: '−';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: #B45309;
    font-size: 10px;
    font-weight: bold;
  }
  
  .window-control-maximize:hover::after {
    content: '+';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: #15803D;
    font-size: 10px;
    font-weight: bold;
  }
  
  .window-controls {
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    padding: 2px 4px;
    border-radius: 8px;
    background: rgba(0, 0, 0, 0.05);
  }

  /* Animaciones estilo macOS para window controls */
  .project-item.closing {
    animation: macosClose 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
    z-index: 1000;
  }
  
  .project-item.minimizing {
    animation: macosMinimize 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
    z-index: 1000;
  }
  
  .project-item.maximizing {
    animation: macosMaximize 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
  }
  
  /* Animación de cerrar - mover hacia posición absoluta usando custom properties */
  @keyframes macosClose {
    0% {
      transform: scale(1) translateY(0);
      opacity: 1;
      
    }
    100% {
      transform: scale(0.8) translateY(-1000px);
    }
  }
  
  /* Animación de minimizar - se vuelve muy pequeña */
  @keyframes macosMinimize {
    0% {
      transform: scale(1) translateY(0);
      opacity: 1;
    }
    100% {
      transform: scale(0.15) translateY(0);
      opacity: 0.7;
    }
  }
  
  /* Animación de maximizar - se vuelve más grande */
  @keyframes macosMaximize {
    0% {
      transform: scale(1) translateY(0);
      opacity: 1;
    }
    100% {
      transform: scale(1.8) translateY(0);
      opacity: 1;
    }
  }
  
  /* Animación de restaurar tarjeta - Simplificada para usar transición CSS */
  .project-item.restoring {
    /* La transición CSS base se encarga de la animación */
  }
  
  /* Keyframe mantenido solo como referencia pero no se usa */
  @keyframes macosRestore {
    0% {
      opacity: 0.8;
    }
    100% {
      transform: scale(1) translate(0, 0);
      opacity: 1;
    }
  }
  
  /* Estados finales para mantener posición */
  .project-item.closed-state {
    cursor: pointer;
    /* Usar transform + opacity (animables) para permitir una transición suave al restaurar */
    transform: translateY(-1000px) scale(0.8);
    opacity: 0;
    pointer-events: none;
  }

  /* Sugerencia de rendimiento: indicar propiedades que cambiarán */
  .project-item,
  .project-item .project-item-inner {
    will-change: transform, opacity;
  }
  
  .project-item.minimized-state {
    transform: scale(0.15) translateY(0) !important;
    opacity: 0.7 !important;
    z-index: 1000;
    cursor: pointer;
  }
  
  .project-item.maximized-state {
    transform: scale(1.8) translateY(0) !important;
    opacity: 1 !important;
    z-index: 1000;
    cursor: pointer;
  }

  /* Estilos para el botón macOS en la esquina inferior derecha */
  .macos-restore-button {
    position: fixed;
    right: 1.25rem;
    bottom: 1.25rem;
    z-index: 1200;
    width: auto;
    height: 56px;
    border-radius: 9999px;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 8px 12px;
    background: linear-gradient(180deg, rgba(255,255,255,0.42), rgba(245, 245, 249, 0.763));
    box-shadow: 0 12px 36px rgba(11, 11, 13, 0.18), inset 0 1px 0 rgba(255,255,255,0.6);
    border: 1px solid rgba(255,255,255,0.35);
    backdrop-filter: blur(10px) saturate(120%);
    -webkit-backdrop-filter: blur(10px) saturate(120%);
    cursor: pointer;
    transition: transform 0.18s ease, box-shadow 0.18s ease, opacity 0.16s ease;
    opacity: 0.98;
    pointer-events: auto;
  }

  .macos-restore-button-inner {
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  .macos-restore-icon {
    width: 18px;
    height: 18px;
    stroke: url(#fake) !important; /* fallback; we'll tint via CSS */
    filter: drop-shadow(0 4px 8px rgba(0,0,0,0.12));
  }

  .macos-restore-label {
    font-family: "Pixelify Sans", cursive;
    font-weight: 700;
    font-size: 0.9rem;
    line-height: 1;
    display: inline-block;
    background: linear-gradient(45deg, #ff9a9e, #fad0c4, #a1c4fd, #c2e9fb);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
    text-shadow: 0 2px 8px rgba(0,0,0,0.06);
    transform: translateY(0);
  }

  /* Ajustes de hover para enfatizar el efecto glass + gradiente */
  .macos-restore-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 18px 44px rgba(11,11,13,0.22), inset 0 1px 0 rgba(255,255,255,0.7);
  }

  .macos-restore-button.show {
    display: inline-flex !important;
    animation: popIn 220ms cubic-bezier(0.2, 0.8, 0.2, 1);
  }

  @keyframes popIn {
    from { transform: scale(0.86) translateY(8px); opacity: 0; }
    to { transform: scale(1) translateY(0); opacity: 1; }
  }
</style>